export const getLogos = (name) => {
  let link = `/icons/logos/${name}`;
  return link;
};

export const getSocial = (name) => {
  let link = `/icons/social/${name}`;
  return link;
};

export const getButton = (name) => {
  let link = `/icons/buttons/${name}`;
  return link;
};

export const getBanner = (name) => {
  let link = `/images/banner/${name}`;
  return link;
};
